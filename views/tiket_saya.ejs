<!DOCTYPE html>
<html lang="id">
<head>
    <title>Tiket Saya</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .ticket-card {
            border-left: 5px solid #0d6efd; /* Garis biru di kiri */
            transition: transform 0.2s;
        }
        .ticket-card:hover {
            transform: translateY(-5px); /* Efek melayang dikit pas disorot */
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        }
        .dashed-line {
            border-top: 2px dashed #ccc;
            margin: 15px 0;
        }
    </style>
</head>
<body class="bg-light">

    <nav class="navbar navbar-dark bg-primary mb-4">
        <div class="container">
            <a class="navbar-brand" href="/">Event System</a>
            <a href="/" class="btn btn-outline-light btn-sm">Kembali ke Beranda</a>
        </div>
    </nav>

    <div class="container">
        <h2 class="mb-4">🎟️ Tiket Saya</h2>

        <% if (tickets.length > 0) { %>
            <div class="row">
                <% tickets.forEach(tiket => { %>
                    <div class="col-md-6 mb-4">
                        <div class="card ticket-card h-100 shadow-sm">
                            <div class="card-body">
                                <div class="d-flex justify-content-between align-items-start">
                                    <div>
                                        <span class="badge bg-success mb-2"><%= tiket.status.toUpperCase() %></span>
                                        <h5 class="card-title fw-bold"><%= tiket.event_title %></h5>
                                    </div>
                                    <div class="text-end">
                                        <small class="text-muted">Kode Booking</small>
                                        <div class="fw-bold text-primary" style="font-family: monospace; font-size: 1.1em;">
                                            <%= tiket.booking_code %>
                                        </div>
                                    </div>
                                </div>

                                <div class="dashed-line"></div>

                                <div class="row text-secondary small">
                                    <div class="col-6">
                                        <p class="mb-1">📅 <b>Tanggal:</b><br>
                                            <%= new Date(tiket.start_date).toLocaleDateString('id-ID', {day: 'numeric', month: 'long', year: 'numeric'}) %>
                                        </p>
                                    </div>
                                    <div class="col-6">
                                        <p class="mb-1">📍 <b>Lokasi:</b><br>
                                            <%= tiket.venue_name %>
                                        </p>
                                    </div>
                                    <div class="col-6 mt-2">
                                        <p class="mb-0">🎫 <b>Jenis Tiket:</b><br>
                                            <%= tiket.ticket_name %>
                                        </p>
                                    </div>
                                    <div class="col-6 mt-2">
                                        <p class="mb-0">💰 <b>Harga:</b><br>
                                            Rp <%= tiket.price.toLocaleString('id-ID') %>
                                        </p>
                                    </div>
                                </div>

                                <div class="mt-4 d-grid gap-2">
                                    <button onclick="window.print()" class="btn btn-outline-primary">
                                        🖨️ Cetak / Simpan PDF
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } else { %>
            <div class="text-center py-5">
                <div class="display-1 text-muted">🎫</div>
                <h4 class="text-muted mt-3">Belum ada tiket yang dibeli.</h4>
                <p>Yuk cari acara seru dan daftar sekarang!</p>
                <a href="/" class="btn btn-primary mt-2">Cari Acara</a>
            </div>
        <% } %>
    </div>

</body>
</html>