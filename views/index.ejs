<!DOCTYPE html>
<html lang="id">
<head>
    <title>Event System</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    
    <nav class="navbar navbar-dark bg-primary mb-4 shadow-sm">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/">Event System</a>
            
            <div class="d-flex align-items-center">
                <% if (user) { %>
                    <span class="text-white me-3 d-none d-md-block">Halo, <b><%= user.name %></b>!</span>
                    
                    <a href="/tiket-saya" class="btn btn-light text-primary btn-sm me-2 fw-bold">
                        🎟️ Tiket Saya
                    </a>

                    <a href="/profile/edit" class="btn btn-info btn-sm me-2 fw-bold text-white">
                        ✏️ Edit Profil
                    </a>

                    <% if (user.role === 'admin' || user.role === 'organizer') { %>
                        <a href="/admin" class="btn btn-warning btn-sm me-2 fw-bold text-dark">
                            📊 Dashboard
                        </a>
                    <% } %>
                    
                    <a href="/logout" class="btn btn-danger btn-sm">Logout</a>

                <% } else { %>
                    <a href="/login" class="btn btn-light btn-sm fw-bold">Login / Daftar</a>
                <% } %>
            </div>
        </div>
    </nav>

    <div class="container pb-5">
        <h3 class="mb-4 fw-bold border-bottom pb-2">📅 Daftar Acara Terbaru</h3>
        
        <div class="row">
            <% if(events.length > 0) { %>
                <% events.forEach(event => { %>
                    <div class="col-md-4 mb-4">
                        <div class="card shadow-sm h-100 border-0">
                            
                            <% if(event.image && event.image !== 'default.jpg') { %>
                                <img src="/uploads/<%= event.image %>" class="card-img-top" alt="<%= event.title %>" style="height: 200px; object-fit: cover;">
                            <% } else { %>
                                <div class="bg-secondary text-white d-flex align-items-center justify-content-center" style="height: 200px;">
                                    <span class="fs-5">No Image</span>
                                </div>
                            <% } %>

                            <div class="card-body d-flex flex-column">
                                <div>
                                    <span class="badge bg-info text-dark mb-2"><%= event.category_name %></span>
                                    <h5 class="card-title fw-bold"><%= event.title %></h5>
                                    <h6 class="card-subtitle mb-3 text-muted">
                                        🗓️ <%= new Date(event.start_date).toLocaleDateString('id-ID', { day: 'numeric', month: 'long', year: 'numeric' }) %>
                                    </h6>
                                    <p class="card-text text-secondary small">
                                        📍 <%= event.venue_name %>
                                    </p>
                                </div>
                                <div class="mt-auto">
                                    <a href="/event/<%= event.event_id %>" class="btn btn-primary w-100 fw-bold">Lihat Detail &rarr;</a>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }) %>
                <% } else { %>
                <div class="col-12">
                    <div class="alert alert-info text-center py-5">
                        <h4>Belum ada acara yang tersedia saat ini.</h4>
                        <p>Silakan cek kembali nanti.</p>
                    </div>
                </div>
            <% } %>
        </div>
    </div>

</body>
</html>